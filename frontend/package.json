{
  "name": "@shopgate/bigcommerce-cart",
  "version": "1.0.1",
  "description": "Shopgate Cloud BigCommerce Cart",
  "scripts": {
    "lint": "eslint --ignore-path ../.gitignore --ext .js --ext .jsx .",
    "test": "jest",
    "test:watch": "jest --watch",
    "coverage": "jest --coverage",
    "coverage:watch": "jest --coverage --watch"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/shopgate/ext-bigcommerce-cart.git"
  },
  "license": "Apache-2.0",
  "dependencies": {},
  "devDependencies": {
    "@shopgate/eslint-config": "^5.3.0",
    "@shopgate/pwa-common": "^5.3.0",
    "@shopgate/pwa-common-commerce": "^5.3.0",
    "@shopgate/pwa-core": "^5.3.0",
    "@shopgate/pwa-unit-test": "^5.3.0",
    "@shopgate/tracking-core": "^5.3.0",
    "eslint": "^4.19.1",
    "jest": "^23.1.0"
  }
}
